using System;
using System.Collections.Generic;
using System.Text;

namespace PhoenixBot.Features.Games.Hunt
{
    public static class HuntListPick
    {/*
        internal static Noob.MonsterInfo Noob()
        {
            var count = Hunt.Noob.NoobMonsterList.monsterInfo.Count;
            Random list = new Random();
            var pick = list.Next(0, count);
            var monster = (Noob.MonsterInfo)Hunt.Noob.NoobMonsterList.monsterInfo.ToArray().GetValue(pick);
            return monster;
        }
        internal static Novice.MonsterInfo Novice()
        {
            var count = Hunt.Novice.NoviceMonsterList.monsterInfo.Count;
            Random list = new Random();
            var pick = list.Next(0, count);
            var monster = (Novice.MonsterInfo)Hunt.Novice.NoviceMonsterList.monsterInfo.ToArray().GetValue(pick);
            return monster;
        }
        internal static Intermediate.MonsterInfo Intermediate()
        {
            var count = Hunt.Intermediate.MonsterList.monsterInfo.Count;
            Random list = new Random();
            var pick = list.Next(0, count);
            var monster = (Intermediate.MonsterInfo)Hunt.Intermediate.MonsterList.monsterInfo.ToArray().GetValue(pick);
            return monster;
        }
        internal static Master.MonsterInfo Master()
        {
            var count = Hunt.Master.MasterMonsterList.monsterInfo.Count;
            Random list = new Random();
            var pick = list.Next(0, count);
            var monster = (Master.MonsterInfo)Hunt.Master.MasterMonsterList.monsterInfo.ToArray().GetValue(pick);
            return monster;
        }
        internal static GrandMaster.MonsterInfo GrandMaster()
        {
            var count = Hunt.GrandMaster.GMMonsterList.monsterInfo.Count;
            Random list = new Random();
            var pick = list.Next(0, count);
            var monster = (GrandMaster.MonsterInfo)Hunt.GrandMaster.GMMonsterList.monsterInfo.ToArray().GetValue(pick);
            return monster;
        }
        internal static Phoenix.MonsterInfo Phoenix()
        {
            var count = Hunt.Phoenix.MonsterList.monsterInfo.Count;
            Random list = new Random();
            var pick = list.Next(0, count);
            var monster = (Phoenix.MonsterInfo)Hunt.Phoenix.MonsterList.monsterInfo.ToArray().GetValue(pick);
            return monster;
        }
    } */
    }
}
